import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  LogoutButtonStyle: {
    borderColor: 'skyblue',
    borderWidth: 2,
    width: 80,
    height: 30,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 10,
    margin: 5,
  },
});
